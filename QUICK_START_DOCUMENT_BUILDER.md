# 🚀 Быстрый старт: Document Builder & Система подписей

## ⚡ Первые шаги

### 1. Применить миграцию базы данных

```bash
# Перейдите в директорию проекта
cd C:\1031-exchange-crm

# Примените миграцию
supabase db push
```

Миграция создаст все необходимые таблицы:
- ✅ `document_template_components`
- ✅ `document_templates`
- ✅ `template_signature_fields`
- ✅ `vesting_name_signatures`
- ✅ `admin_signatures`
- ✅ `documents`
- ✅ `document_signature_requests`

### 2. Перезапустить приложение

```bash
npm run dev
```

### 3. Проверить навигацию

Откройте приложение и проверьте:
- ✅ В левой навигации появилась иконка "Document Builder" (📄 с плюсом)
- ✅ Переход на `/document-builder` работает

---

## 📝 Как создать первый шаблон

### Шаг 1: Откройте Document Builder
Кликните на иконку Document Builder в левой панели или перейдите на `/document-builder`

### Шаг 2: Выберите тип объекта
Выберите один из типов:
- **Transaction** 📄 - для транзакций
- **Exchange** 🔄 - для обменов
- **Property** 🏠 - для недвижимости
- **EAT** 📋 - для EAT парковок

### Шаг 3: Создайте шаблон
1. Нажмите "Создать шаблон"
2. Введите название (например: "Purchase Agreement")
3. Добавьте описание (опционально)

### Шаг 4: Используйте редактор
**Форматирование:**
- **B** - Жирный текст
- *I* - Курсив
- <u>U</u> - Подчеркнутый
- Выравнивание: ← ↔ → ⇔
- Списки: маркированные и нумерованные

**Динамические поля:**
Справа отображаются доступные поля для выбранного типа объекта.
Кликните на поле, чтобы вставить его в документ.

Примеры:
- `<<transaction number>>` - вставит номер транзакции
- `<<seller name>>` - вставит имя продавца
- `<<property address>>` - вставит адрес недвижимости

### Шаг 5: Добавьте поля для подписей (опционально)
1. Нажмите кнопку "✍️ Подписи"
2. Нажмите "Добавить поле"
3. Настройте параметры поля:
   - Название (например: "Seller Signature")
   - Тип (signature, date, text)
   - Порядок подписания
   - Роль подписанта

### Шаг 6: Сохраните
Нажмите "Сохранить шаблон"

---

## ✍️ Как настроить подписи

### Для пользователей (Vesting Names)

#### В настройках Tax Account:

```tsx
// Добавьте компонент в настройки tax account
import SignatureManager from '@/components/SignatureManager';

<SignatureManager 
  taxAccountId={taxAccount.id}
  vestingNames={taxAccount.vesting_names}
/>
```

**Пользователь может:**
1. Выбрать vesting name
2. Выбрать тип подписи:
   - **Property**: Подпись + Printed Name
   - **Entity**: Entity Name + Подпись + BY + ITS
3. Ввести текст подписи
4. Выбрать шрифт из списка
5. Просмотреть предпросмотр
6. Сохранить подпись

**Каждая подпись получает уникальный ID** (например: `SIG-A3B9C2D1E4F5`)

### Для администраторов QI

#### В админке QI:

```tsx
// Добавьте компонент в админку QI (где список админов)
import AdminSignatureManager from '@/components/AdminSignatureManager';

<AdminSignatureManager 
  qiCompanyId={qiCompany.id}
  adminUsers={adminsList}
/>
```

**Владелец QI может:**
1. Выбрать администратора из списка
2. Настроить подпись аналогично пользовательской
3. Сохранить подпись для админа

---

## 📄 Как добавить документы на страницу транзакции

### В файл страницы транзакции:

Найдите файл страницы транзакции (например: `src/app/(pages)/transactions/[id]/page.tsx`)

Добавьте компонент:

```tsx
import TransactionDocuments from '@/components/TransactionDocuments';

// В JSX компонента страницы транзакции
<TransactionDocuments transactionId={transaction.id} />
```

**Что получите:**
- Блок "Document Templates"
- Кнопка "Create Template"
- Список созданных документов со статусами
- Модальное окно выбора шаблона
- Переход в Document Builder для создания нового шаблона

---

## 🎯 Типовой сценарий использования

### Сценарий: Создание Purchase Agreement для транзакции

#### 1. Создание шаблона (Админ)
1. Откройте Document Builder
2. Выберите "Transaction"
3. Создайте шаблон "Purchase Agreement"
4. Добавьте текст договора с динамическими полями:
   ```
   PURCHASE AGREEMENT
   
   Date: <<contract date>>
   Transaction: <<transaction number>>
   
   SELLER: <<seller name>>
   BUYER: <<buyer name>>
   
   PROPERTY ADDRESS: <<property address>>
   PURCHASE PRICE: $<<contract price>>
   
   [Условия договора...]
   ```
5. Добавьте 2 поля для подписей:
   - "Seller Signature" (signing_order: 1)
   - "Buyer Signature" (signing_order: 2)
6. Сохраните шаблон

#### 2. Настройка подписей (Пользователи)
Каждый пользователь в настройках своего Tax Account:
1. Создает подпись для своего vesting name
2. Выбирает тип (Property или Entity)
3. Настраивает внешний вид подписи

#### 3. Создание документа (Админ)
На странице транзакции:
1. Нажмите "Create Template"
2. Выберите "Purchase Agreement"
3. Система создаст документ и заполнит все поля `<<...>>`
4. Документ появится в списке со статусом "Draft"

#### 4. Отправка на подпись (Будущая функциональность)
После заполнения документа:
1. Выберите подписантов из админов/пользователей
2. Отправьте документ на подпись
3. Отслеживайте статус подписей
4. После всех подписей документ получит статус "Completed"

---

## 🔍 Где что находится

### Файлы проекта:

**SQL Миграция:**
```
supabase/migrations/038_create_document_builder_and_signatures_system.sql
```

**Типы и константы:**
```
src/types/document.types.ts
```

**API библиотека:**
```
src/lib/document-templates.ts
```

**Компоненты:**
```
src/components/DocumentTemplateEditor/      # Редактор шаблонов
src/components/SignatureManager/            # Подписи пользователей
src/components/AdminSignatureManager/       # Подписи админов
src/components/TransactionDocuments/        # Документы на странице транзакции
```

**Страницы:**
```
src/app/(pages)/document-builder/page.tsx   # Главная страница Document Builder
```

**Навигация:**
```
src/layout/Sidebar/Sidebar.tsx              # Обновлен для добавления иконки
```

---

## ✅ Чек-лист для проверки

После установки проверьте:

- [ ] Миграция применена (`supabase db push`)
- [ ] В навигации появилась иконка Document Builder
- [ ] Страница `/document-builder` открывается
- [ ] Можно выбрать тип объекта
- [ ] Можно создать шаблон
- [ ] Редактор текста работает (форматирование)
- [ ] Динамические поля вставляются
- [ ] Компонент TransactionDocuments добавлен на страницу транзакции
- [ ] Компонент SignatureManager добавлен в настройки профиля
- [ ] Компонент AdminSignatureManager добавлен в админку QI

---

## 📚 Документация

Полная документация: `DOCUMENT_BUILDER_SYSTEM_README.md`

---

## 🐛 Проблемы?

### Миграция не применяется
```bash
# Проверьте подключение к Supabase
supabase status

# Попробуйте снова
supabase db push
```

### Компоненты не импортируются
```bash
# Перезапустите dev сервер
npm run dev
```

### Ошибки TypeScript
```bash
# Проверьте типы
npm run type-check
```

---

**Готово!** 🎉

Теперь у вас есть полнофункциональная система для создания документов с динамическими полями и цифровыми подписями!

Для дополнительных вопросов смотрите полную документацию в `DOCUMENT_BUILDER_SYSTEM_README.md`.

