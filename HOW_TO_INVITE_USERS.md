# Инструкция по приглашению пользователей к профилям

## Обзор

Система позволяет администраторам приглашать новых пользователей и автоматически привязывать их к существующим профилям. Это полезно, когда у вас есть профиль в системе, но еще нет связанного аккаунта пользователя.

## Что было реализовано

### 1. База данных

Создана таблица `profile_invitations` для хранения приглашений:
- **profile_id** - ID профиля, к которому будет привязан пользователь
- **email** - Email приглашенного пользователя
- **token** - Уникальный токен для регистрации (генерируется автоматически)
- **status** - Статус приглашения (pending, accepted, expired)
- **invited_by** - ID администратора, создавшего приглашение
- **created_at** - Дата создания
- **expires_at** - Дата истечения (по умолчанию 7 дней)

### 2. Функционал на странице профиля

На странице просмотра профиля (`/profiles/[id]`) для администраторов доступен функционал приглашения:
- Кнопка "📧 Пригласить к регистрации" (отображается только для админов)
- Форма для ввода или подтверждения email
- Отображение активного приглашения с возможностью скопировать ссылку
- Приглашение отображается только если профиль еще не привязан к пользователю (`user_id` пуст)

### 3. Страница регистрации по приглашению

Создана страница `/register/[token]` для регистрации новых пользователей:
- Проверка валидности токена и срока действия
- Отображение информации о профиле
- Форма регистрации с email (предзаполнен из приглашения)
- Автоматическая привязка `user_id` к профилю после регистрации
- Создание записи в `user_profiles` с ролью `user`

## Как использовать

### Шаг 1: Запустите миграцию

```bash
# Выполните SQL миграцию в Supabase Dashboard
# Откройте SQL Editor и выполните файл:
# supabase/migrations/006_create_profile_invitations.sql
```

Или выполните SQL напрямую через Supabase Dashboard:
1. Откройте https://supabase.com/dashboard/project/YOUR_PROJECT_ID
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое файла `supabase/migrations/006_create_profile_invitations.sql`
4. Вставьте и нажмите **Run**

### Шаг 2: Войдите как администратор

Только пользователи с ролями `workspace_owner`, `platform_super_admin` или `admin` могут создавать приглашения.

### Шаг 3: Откройте страницу профиля

1. Перейдите в раздел "Profiles": http://localhost:3000/profiles
2. Выберите профиль, который еще не привязан к пользователю
3. Откройте страницу профиля

### Шаг 4: Создайте приглашение

1. Нажмите кнопку "📧 Пригласить к регистрации"
2. Введите или подтвердите email для приглашения
3. Нажмите "Создать приглашение"
4. Система сгенерирует уникальную ссылку для регистрации

### Шаг 5: Поделитесь ссылкой

1. Нажмите кнопку "📋 Копировать ссылку" чтобы скопировать ссылку в буфер обмена
2. Отправьте ссылку пользователю (по email, мессенджеру и т.д.)
3. Ссылка имеет формат: `http://your-domain.com/register/[token]`

### Шаг 6: Пользователь проходит регистрацию

1. Пользователь открывает полученную ссылку
2. Видит информацию о профиле, к которому будет привязан
3. Вводит пароль (email уже предзаполнен)
4. Нажимает "Завершить регистрацию"
5. После регистрации:
   - Создается аккаунт в системе аутентификации
   - `user_id` автоматически привязывается к профилю
   - Создается запись в `user_profiles` с ролью `user`
   - Статус приглашения меняется на `accepted`

### Шаг 7: Подтверждение email

1. Пользователь получает письмо от Supabase для подтверждения email
2. После подтверждения аккаунт становится полностью активным

## Безопасность и ограничения

### Права доступа (RLS)

- **Создание приглашений**: только админы (`workspace_owner`, `platform_super_admin`, `admin`)
- **Чтение приглашений**: все могут читать активные приглашения по токену
- **Обновление приглашений**: только админы
- **Удаление приглашений**: только `workspace_owner`

### Ограничения

- Приглашение действительно 7 дней с момента создания
- Одновременно может быть только одно активное приглашение для профиля
- Приглашение можно создать только для профилей без привязанного `user_id`
- После принятия приглашения статус меняется на `accepted` и его нельзя использовать повторно

## Проверка работы

### Проверка в базе данных

```sql
-- Просмотр всех приглашений
SELECT 
    pi.email,
    pi.status,
    pi.created_at,
    pi.expires_at,
    p.first_name,
    p.last_name
FROM profile_invitations pi
JOIN profile p ON p.id = pi.profile_id
ORDER BY pi.created_at DESC;

-- Просмотр профилей с привязанными пользователями
SELECT 
    id,
    first_name,
    last_name,
    email,
    user_id,
    CASE 
        WHEN user_id IS NOT NULL THEN 'Привязан'
        ELSE 'Не привязан'
    END as status
FROM profile
ORDER BY created_at DESC;
```

## Структура файлов

```
src/app/
├── (pages)/profiles/[id]/page.tsx       # Добавлена кнопка Invite и логика
├── register/[token]/page.tsx            # Новая страница регистрации
supabase/migrations/
└── 006_create_profile_invitations.sql   # Миграция для таблицы приглашений
```

## Что дальше?

После внедрения функционала приглашений вы можете:

1. **Добавить отправку email**: Интегрировать отправку писем с ссылкой-приглашением
2. **Добавить историю приглашений**: Показывать список всех приглашений на странице профиля
3. **Добавить возможность отмены**: Позволить отменять неиспользованные приглашения
4. **Добавить уведомления**: Уведомлять админов о принятых приглашениях

## Решение проблем

### Приглашение не создается

- Убедитесь, что вы залогинены как администратор
- Проверьте, что профиль не имеет привязанного `user_id`
- Проверьте логи консоли браузера на наличие ошибок

### Ссылка приглашения не работает

- Убедитесь, что токен корректный
- Проверьте, что приглашение не истекло (смотрите `expires_at`)
- Убедитесь, что статус приглашения `pending`

### После регистрации user_id не привязывается

- Проверьте RLS политики для таблицы `profile`
- Убедитесь, что у таблицы есть права на обновление
- Проверьте логи Supabase на наличие ошибок

## Важные замечания

1. **Email в профиле**: Если у профиля уже есть email, он будет использоваться по умолчанию для приглашения
2. **Роль пользователя**: Регистрация через приглашение создает обычного пользователя (роль `user`), а не администратора
3. **Уникальность email**: Один email может быть использован только для одного аккаунта
4. **Срок действия**: Приглашения автоматически становятся неактивными через 7 дней

## Контакты

Если у вас возникли вопросы или проблемы, пожалуйста, обратитесь к администратору системы.


