# Инструкция по настройке системы управления администраторами

## Что было внедрено

### 1. Система ролей

- **workspace_owner** - единственный владелец воркспейса (fdgventures@gmail.com)
- **platform_super_admin** - супер администраторы (могут создавать админов)
- **admin** - обычные администраторы (доступ к админ панели)

### 2. Функционал

- ✅ Управление администраторами через админ панель
- ✅ Создание приглашений для новых админов
- ✅ Система привязки профилей к пользователям
- ✅ Автоматическая установка fdgventures@gmail.com как workspace_owner
- ✅ Ограничение на создание только одного workspace_owner

## Шаги для запуска

### 1. Запустите SQL миграцию в Supabase

1. Откройте Supabase Dashboard: https://supabase.com/dashboard/project/zaclmrvdnydyqmeiorze
2. Перейдите в **SQL Editor**
3. Откройте файл `supabase/migrations/001_admin_system.sql`
4. Скопируйте весь SQL код
5. Вставьте в SQL Editor и нажмите **Run**

### 2. Запустите приложение

```bash
npm run dev
```

### 3. Зарегистрируйте workspace_owner

1. Откройте http://localhost:3000/admin/register
2. Используйте email: **fdgventures@gmail.com**
3. Создайте пароль (минимум 6 символов)
4. Подтвердите email по ссылке из письма

После регистрации fdgventures@gmail.com автоматически станет workspace_owner 👑

### 4. Управление администраторами

1. Войдите в админ панель: http://localhost:3000/admin/signin
2. Перейдите в "Admin Management" на дашборде
3. Нажмите "+ Invite New Admin"
4. Введите email и выберите тип роли (Platform Super Admin или Admin)
5. Система автоматически создаст приглашение

### 5. Регистрация по приглашению

После создания приглашения:

1. Админ-администратор (workspace_owner или super admin) копирует токен из базы данных
2. Открывает ссылку: http://localhost:3000/admin/register-invite/[token]
3. Заполняет форму регистрации
4. Подтверждает email
5. После подтверждения пользователь становится активным админом

## Структура файлов

```
src/app/admin/
├── admins/page.tsx                     # Управление админами
├── dashboard/page.tsx                  # Главная админ панель (обновлена)
├── register/page.tsx                   # Регистрация (обновлена для workspace_owner)
├── register-invite/[token]/page.tsx   # Регистрация по приглашению
├── setup/page.tsx                      # Настройка workspace_owner
└── signin/page.tsx                     # Вход (обновлена проверка ролей)

supabase/migrations/
└── 001_admin_system.sql                # SQL миграция
```

## Возможности

### Workspace Owner (fdgventures@gmail.com)

- ✅ Полный доступ ко всем функциям
- ✅ Создание приглашений для админов
- ✅ Удаление приглашений
- ✅ Просмотр всех админов

### Platform Super Admin

- ✅ Доступ к админ панели
- ✅ Создание приглашений для админов
- ✅ Просмотр всех админов
- ❌ Удаление приглашений (только для workspace_owner)

### Admin

- ✅ Доступ к админ панели
- ❌ Управление другими админами
- ❌ Создание приглашений

## Проверка установки

После выполнения миграции выполните SQL запрос:

```sql
SELECT email, role, role_type, is_verified
FROM user_profiles
WHERE email = 'fdgventures@gmail.com';
```

Ожидаемый результат:

- role: `platform_super_admin`
- role_type: `workspace_owner`
- is_verified: `false` (пока не подтвержден email)

## Важные заметки

1. **Только один workspace_owner**: База данных настроена так, что только один пользователь может иметь роль workspace_owner
2. **Автоматическое определение роли**: При регистрации fdgventures@gmail.com автоматически получает роль workspace_owner
3. **Приглашения**: Срок действия приглашений - 7 дней
4. **Права доступа**: RLS политики настроены так, что только workspace_owner и platform_super_admin могут управлять приглашениями
